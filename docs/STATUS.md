# 当前状态

更新时间：2026-08-10

## 已实现

- 单一“启用 StoryEcho 上下文管理”功能开关；
- 完整轮次与消息数两种近期窗口；
- 安全覆盖边界：未总结原文不裁剪；
- 原文阶段总结（L1）、来源哈希与前一总结衔接；
- L1 与 L2+ 独立合并阈值及输出上限；
- 同层最老一组递归压缩、跨层级级联和 `O(log N)` 总结前沿；
- 高层直接子来源快照、重新生成和原子提交；
- 显式剧情阶段边界隔离；
- 蓝灯常驻与批次命中绿灯世界书背景；
- 所有层级的搜索、分页、编辑、删除与重新生成；
- 一键整理层级与完整重建；
- 编辑、删楼、Swipe、分支与聊天重命名失效处理；
- 前台/手动/后台统一任务队列、生成后租约和 AbortSignal；
- SillyTavern 主连接和自定义 OpenAI 兼容 LLM；
- 空响应重试、超时重试和可选主连接回退；
- 最近一次真实请求 Token 构成；
- 运行指标、最近检查记录、调试轨迹和脱敏报告；
- Schema 12 设置与 Schema 3 聊天状态；
- 旧总结迁移为 L1、旧全局骨架丢弃；
- source map、manifest 静态资源查询参数和 bundle 构建。

## 已移除

- 全局剧情骨架及其首次构建、增量吸收和 stale 状态；
- 剧情记忆开关及元数据管理器；
- 结构化剧情抽取与整理；
- 当前状态校正；
- 查询改写、排序和动态召回；
- Embedding Provider；
- Vector Storage 适配、集合注册表和同步队列；
- 结构化 JSON 输出协商、修复与诊断。

## 验证基线

```bash
npm run typecheck
npm test
npm run build
npm run test:coverage
```

当前功能测试覆盖窗口、调度、拦截安全边界、L1 生成、层级进位与级联、来源竞态、世界书、LLM 超时与取消、状态迁移、Token 分类和 UI 契约。

## 已知边界

- 首次生成 L1 与各级压缩仍需要可用 LLM；
- 后台每次只处理一个 L1 和一个压缩组，大量积压会分多轮追赶；
- 高层重新生成依赖持久化的直接子总结快照，不会重新展开完整原文提示；
- 世界书参考读取依赖 SillyTavern 公开接口或兼容模块；
- Token 卡片依赖 SillyTavern 保存的最近提示词明细；缺失分类计数时会明确显示估算；
- 临时聊天离开后由 SillyTavern 删除，其 StoryEcho 聊天元数据随聊天一起消失。
