# 当前实现状态

更新时间：2026-07-19

## 已完成

- SillyTavern 第三方扩展 manifest、TypeScript 构建和 Git 安装产物；
- 可独立折叠的分区卡片式设置面板、酒馆助手风格滑动开关和响应式操作按钮；
- 最近窗口按轮次或消息计算；
- 默认主连接 LLM Provider；
- 自定义 OpenAI 兼容 LLM，经 SillyTavern 自带后端转发；
- 自定义 LLM 失败后可回退主连接；
- 自定义 OpenAI 兼容 Embedding；
- 火山方舟多模态 Embedding，支持专用请求/响应格式、4路并发和旧方舟Key迁移；
- 自定义 Embedding 自动经 SillyTavern 内置代理转发、连接测试和响应校验；
- LLM 与 Embedding Key 通过 `extensionSettings` 持久化和同步；
- 远程预生成向量写入 Vector Storage，继续由酒馆服务端保存和检索；
- 剧情抽取 Prompt、JSON Schema 和响应校验；
- 多实体复合候选的确定性原子化，避免同名或相近地点中的独立事实被绑定为一条记忆；
- 普通低价值事件过滤、结构完整度提权、无源文本证据的伪“未解决线索”清理和候选拒绝原因调试；
- 历史切块和手动处理窗口外历史；
- 剧情事件写入 `chatMetadata`；
- CREATE/MERGE/UPDATE/RESOLVE/SUPERSEDE/IGNORE 六类事件整理；
- 同一事实移动时自动取代旧状态，补充确认时合并且保留原有原因与结果；
- 取代旧版复合记忆中的单个事实时保留可安全拆出的其余事实；
- 已完成承诺和已否定传言清除旧待办，并作为当前有效事实参与排序而不统一降权；
- LLM 整理失败时的保守规则回退和手工编辑保护；
- 记忆更新后的旧向量删除与新向量增量同步；
- 每聊天/分支独立 Vector Storage 集合；
- 已索引前缀指纹校验；切分支、编辑、删楼或 Swipe改变历史后自动清空派生记忆与旧向量并从原始聊天重建；
- Embedding 来源、端点或模型变化检测与自动重建；
- 有覆盖游标和来源哈希复核的单份滚动阶段总结；
- 默认每 10 个完整“用户 + AI”轮次自动更新一批，主动处理历史时允许追赶不足整批的尾段；
- 生成前安全检查、单分块追赶索引与总结、窗口裁剪和记忆块注入；
- 阶段总结位于近期原文前、动态召回位于当前 User 前的请求级 narrator/system 注入顺序；
- 总结与召回只修改本次请求副本，不写入聊天 UI/JSONL，也不修改当前用户消息；
- 总结或索引落后时保留所有未覆盖原文，并与最小 N 轮窗口取并集；
- 合并记忆按全部来源范围判断窗口外资格，避免最新确认遮蔽较早关键事实；
- LLM 结合最近 3 条上下文生成检索查询、页面内存缓存和本地规则回退；
- 主连接后台任务临时降低推理强度，正常角色生成设置保持不变；
- 用户意图与 AI 场景尾部双路召回、弱语义检测、实体降级匹配、当前状态优先和轻量重排；
- 使用结构化事件、当前结果、实体与知情范围渲染召回块，并显示实际渲染内容；
- 长聊天窗口单次扫描与稳定数组压缩、最多200条Token统计采样、空向量队列快速返回和向量计数按需刷新；
- 501楼/300条记忆的本地性能与元数据体积回归测试；
- 五轮高密度抽取使用8192输出Token上限，避免完整结构化JSON被截断；
- 抽取、整理、向量、裁剪、召回统计和脱敏调试报告；
- 设置、查询改写、整理、向量、Embedding、LLM 代理、哈希、URL、切块、解析、窗口和排序测试。

## 2026-07-18 真实页面验证

- 在 Assistant 独立聊天中写入68条消息，其中包含地点转移、持有者变化、秘密知情者、承诺完成、否定事实和两个同名“白塔”，并以22轮无关日常把关键事实移出最近20轮；
- 抽取得到9条记忆：4条active、2条resolved、3条superseded；有效6条全部完成向量同步；
- 第一组实际角色生成准确回答星纹罗盘新位置与旧址为空、苏棠成为唯一持有人、白塔药铺与北境白塔没有串线；
- 第二组最终召回块同时包含陆遥/叶澄知情、蓝皮账册承诺已完成、木箱不存在黑色封条三项当前事实；修复前黑色封条记忆曾因resolved统一降权被挤出前5；
- 总开关关闭后发起一次真实生成，最近检查时间、查询改写、裁剪、注入和Token统计均未变化；随后已恢复开启；
- 折叠卡片在真实 Chrome 页面完成展开、内容可见、再次收起验证；主连接测试以128输出Token调用`deepseek-v4-flash`并收到`OK`，避免推理模型把16 Token预算全部用于隐藏思考而误报空响应；
- 同名实体回归：在最近2轮窗口下，先写入“白塔药铺的琥珀戒指”和“北境白塔的银铃”，再只移动戒指；窗口外抽取产生两条独立记忆，旧戒指位置被取代，银铃仍有效，最终真实回复为“戒指在白塔药铺后院保险柜，银铃在北境白塔顶层”；
- 分支回归：主线先由红色更新为蓝色并完成入库，再从红色确认楼创建并切入 `Branch #1`；插件生成了独立聊天 UUID和向量集合，调试轨迹确认清除了6条父分支派生记忆，重建后不含任何蓝色记忆，最终真实回复为“红色”；父分支文件仍保持蓝色为 active，两个分支互不污染；
- 删楼回归：让“曜石哨子位于松林岗哨北门抽屉”完成抽取与向量同步，再从酒馆界面删除用户事实楼和 AI确认楼；下一次生成的调试轨迹确认清除了4条派生记忆且向量 `purgeFailed=false`，重建后元数据和向量队列均不含旧位置，最终真实回复为“没有已确认记录”；
- 当前全局小说预设会偶发压过用户要求的诊断输出格式：召回块正确时，`deepseek-v4-flash`仍可能选择续写剧情。这属于下游角色生成的预设服从问题，StoryEcho不会强制改变角色卡或全局创作风格。

## 尚未完成

- 记忆列表、编辑器和完整上下文检查器；
- Vector Storage 全部来源的参数适配；
- 后台低优先级自动抽取调度；
- 精确 Token 计数；
- 更多真实外部 LLM/Embedding 提供方的兼容性测试；
- 正式发布、许可证和升级迁移策略。

## 当前安全默认值

- 扩展默认关闭；
- LLM 默认使用主连接；
- 自定义 Key 明文保存在当前用户扩展设置中，以换取持久化和多端同步；
- Key 不进入聊天数据、Vector Storage 或调试报告；
- 自定义 LLM 由 SillyTavern Chat Completions 后端转发；自定义 Embedding 由内置 `/proxy/` 转发；
- 索引没有覆盖裁剪边界时保留完整聊天；
- 阶段总结没有覆盖裁剪边界时保留未总结原文；
- 单次正常生成最多同步执行一个抽取分块；
- 单次正常生成最多同步执行一个阶段总结分块；
- 检索、抽取或自定义 Provider 失败时优先放行正常生成或按设置回退。
